# GitHub Pages builds this site remotely; this Gemfile documents that and enables
# an optional project-local preview (bundle install --path vendor/bundle) without
# touching system Ruby. It is not required for the Pages build.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
